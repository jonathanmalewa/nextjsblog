/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com"],
  },
}; //untuk menambahkan external domain agar dapat mengexport nya

module.exports = nextConfig;
